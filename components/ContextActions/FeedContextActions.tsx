import { ActionIcon, Container, Flex } from '@mantine/core';

type Props = {
    onSubmit: Function,
};

export default function FeedContextActions(props : Props) {
    return (
        <Container>
            <Flex>

            </Flex>
        </Container>
    );
}
