import { Card, CardSection, Text } from '@mantine/core';
import { Author } from '@/lib/definitions';

export default function AuthorCard(user : Author) {
    return (
        <Card>
            <CardSection>
                <Text></Text>
            </CardSection>
        </Card>
    );
}
